# Web-secure
 A web security scanner built with Python and FastAPI. Combines nmap, SQLMap, SSLyze, and WebTech into one unified REST API that scans targets for open ports, SQL injection, SSL issues, and technology fingerprinting. Scan results are stored in a SQLite database.
